import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    './src/**/*.{js,ts,jsx,tsx,mdx}'
  ],
  "darkMode": "selector",
  theme: {
  },
  plugins: [],
};
export default config;
